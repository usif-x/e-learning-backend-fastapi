[phases.build]
cmds = [
    "pip install -r requirements.txt",
]

# مرحلة ما بعد البناء - تشغيل ال migrations
[phases.postinstall]
cmds = [
    "echo 'Running database migrations...'",
    "alembic upgrade head",
]

[start]
cmd = "python main.py prod --workers 4"